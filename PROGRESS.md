# PROGRESS — LumeMedLink

> El estado real, sin adornos. Si este archivo dice más de lo que el código hace, es un defecto (la
> regla de la familia: la doc nunca queda atrás — ni adelante — del código).

| Fase | Estado | Detalle |
| --- | --- | --- |
| Constitución + ADRs + threat model | ✅ 2026-08-17 | `CLAUDE.md`, ADR-0001…0008, `docs/security/threat-model.md`. |
| S0.1 (esqueleto KMP) | ✅ 2026-08-20 | `:composeApp` (KMP, árbol ADR-0008) + `:androidApp` (shell). `./gradlew clean build` y `compileKotlinIosSimulatorArm64` verdes en esta máquina, catálogo + lockfiles. **CI: corrió UNA vez tras el push del autor y FALLÓ — el runner macos-15 trae Xcode 16.4 y CMP 1.11.1 linkea contra el SDK de iOS 26 (~176 min facturados). Pausado a `workflow_dispatch` (2026-08-21, minutos al 90%); fix escrito (macos-26 + selección explícita de Xcode 26) y SIN verificar hasta el próximo dispatch.** Bitácoras 0002 y 0005. |
| S0.2 (gates) | ✅ 2026-08-20 | detekt + ktlint + 3 scripts (`Scripts/`) + allowlist de dependencias, **cada gate visto rojo con cebo** (el ensayo atrapó un gate ciego de nacimiento). `no_mutable_object`/`no_hardcoded_style` siguen [manual], declarado. Daemon JVM pinneado a 17. Bitácora 0003. |
| Espejo de red (`core/networking`) | ✅ 2026-08-20 | ADR-0004 sobre Ktor: https-only fail-closed ×2, sin redirects, retry sólo-GET acotado, RFC 9457 → `AppError` sin prosa del servidor, seams `TokenProvider`/`NetworkLogSink`, engines OkHttp (TLS≥1.2 explícito)/Darwin. **13 tests × 2 targets verdes** (Android host + simulador iOS). Bitácora 0004. |
| Host iOS (`iosApp/`) | ✅ 2026-08-25 | **Existía sólo el framework; ahora existe la app.** Proyecto Xcode escrito a mano, hermano de `androidApp/` en el mismo repo (no un segundo GitHub). Compila y **corre en simulador**. Lleva las dos colas iOS declaradas: veto de teclados de terceros (§8.10, la única asimetría a favor de iOS) y cover de privacidad en `UIWindow` propia sobre `.alert` armado en `willResignActive`. Entitlement de keychain con grupo **propio, jamás compartido** — LumeMed firma con el mismo equipo y guarda la ficha (ADR-0001). Tres claves AUSENTES del plist como control: ATS, file sharing, custom URL scheme. Gate nuevo `check-ios-host.sh` **con 12 cebos** — el host era un punto ciego que ningún gate veía, cerrado en el mismo commit. **Encontró un crash de arranque**: `App()` dejaba escapar la excepción del Keychain; el disparador observado fue mi build sin firmar (no es condición de producción, y lo verifiqué), pero la clase sí lo es. Arreglado en `app/probeSession`, fuera de la composable, con 3 tests y 3 cebos. **El cover iOS sigue SIN VERIFICAR**: el simulador no puede — con ambos covers desactivados el resultado es idéntico por tres métodos (Metal). ADR-0025, bitácora 0023. |
| Mitad cliente del contrato | 🟡 2026-08-25 | **F23 construido del lado cliente.** `HttpSecurityEventReporter` sobre el stack endurecido, con un **doble del contrato** (servidor guionado detrás del stack **real**, no un mock del reporter) — la forma contra la que se construirá el resto mientras el backend construye lo que ya aceptó. 7 tests: nunca lanza, POST sin reintento (T13), sólo cruza el kind opaco, el log no lo contiene. **Encontró un defecto en código embarcado**: la cancelación del scope del llamador llega **envuelta**, la rama por tipo no la ve, y el llamador recibe un `AppError` por algo que no es un fallo — segundo caso de la misma forma tras F12. Cerrado con `check-cancellation-guard.sh` (ensureActive obligatorio en todo catch amplio; `LumeHttpStack` es la **única** exención, con razón escrita) y el comportamiento del stack **caracterizado por test**, no descrito por comentario. Diagnostiqué mal dos veces por un test pegado en la clase equivocada — octavo verde-por-razón-equivocada, el primero del arnés y no del código. **Falta**: el cableado (sin base URL ni auth) y el esquema del cuerpo, que es de la plataforma (`backend-requests/0004`). ADR-0026, bitácora 0024. |
| **Re-baseline contra contrato 0.32.0** | ✅ 2026-09-07 | **El mapa estaba viejo y decía cuatro cosas falsas.** En 13 días el backend hizo 39 commits y **construyó** `ADR-0035` y `ADR-0036` (2026-08-27), que nuestro plan seguía dando por «aceptados, sin construir». Leído del contrato, no del tablero: 94 rutas, 6 operaciones `/v1/patient/*` + 3 de portal-access, **cero cabeceras de idempotencia** (T13 viva), **ninguna ruta de reagendar** (T7 viva), y **la agenda del día tenant-scoped —la pantalla principal de S1.3— sigue sin existir**. F9 pasó de 🔒 a 🟡; F10/F11/F15/F16 conservan el bloqueo pero con **otra causa, más precisa**: ya no esperan un ADR, esperan un **proyecto GCP que no existe** (ni staging ni producción) o una ruta sin construir. **El re-baseline encontró un defecto propio embarcado** (ver F23). Pedidos 0004 respondido leyendo el contrato; 0006 escrito. ADR nada nuevo: esto corrige el mapa, no decide. Bitácora 0025. |
| F7 · sentinel de instalación iOS | ✅ 2026-09-07 | **La primera propiedad de seguridad iOS que este repo verifica de verdad.** El Keychain de iOS sobrevive a borrar la app: reinstalar heredaba los secretos de quien tuvo el teléfono antes. Cerrado con marcador-archivo en Application Support (sin protección de datos, excluido del backup), orden purgar→marcar, y fallo que deja el contenedor **sin marcar**. `verify-install-sentinel.sh` con **control en vivo que reproduce el agujero**. Android: no-op declarado con su razón. **Y encontró que el host llevaba desde el 2026-08-25 enlazando Kotlin viejo** — la build phase no fijaba `KOTLIN_FRAMEWORK_BUILD_TYPE` y el plugin lo avisa como *warning* mientras el build sigue verde. Eso **invalida toda observación de iOS de esas dos semanas**, y la conclusión de ADR-0025 sobre el cover de privacidad **se retira por no probada** (no se reemplaza: re-medirla quedó inconcluso). Gates: 5 de 6 cebos salieron verdes en la primera pasada porque un *import* o el propio comentario satisfacían el chequeo — corregidos, 8 cebos rojos. ADR-0028, bitácora 0026. |
| **Auditoría exhaustiva + los gates que no gateaban** | ✅ 2026-09-21 | **Auditoría de 13 dimensiones con refutación adversarial (3 lentes por hallazgo): 53 defectos vivos sobrevivieron, 2 murieron.** Dos críticos, misma forma: **un gate verde con su control borrado**. (1) Los cuatro atributos de postura del manifiesto movidos a un comentario XML de cuatro líneas — XML bien formado, `<application>` sin ninguno, `check-network-posture` y `check-backup-posture` **verdes**: F6 y F12 borrables con CI en verde. (2) Las cuatro llamadas de endurecimiento de ventana borradas de `MainActivity` y sus nombres puestos en una `listOf` de un test que no corre — `check-screen-security` y `check-input-surfaces` **verdes**: F1 y F3. **Ninguno era regresión: los dos estaban ciegos desde que nacieron**, y el ensayo con cebo que debía atraparlos sí se hizo — lo hizo quien escribió el gate. Arreglada la **clase** en **seis** gates (los cuatro más `check-preauth-surfaces` y `check-ios-host`) con dos herramientas compartidas en `Scripts/lib/`: un tokenizador que despoja comentarios y literales sin mover los números de línea, y un lector de atributos que **parsea** el XML. Un tercer hueco apareció escribiendo el ensayo: `check-ios-host` exigía que el veto de teclados **existiera**, no que **rechazara** — con `return true` seguía verde. **El ensayo pasa de acto a artefacto**: `Scripts/rehearse-gates.sh`, 15 cebos, los 15 rojos, en CI después de los gates que ensaya. El propio ensayo mintió primero (medía `HEAD` con los arreglos sin commitear: 11 falsos «no detectado»). **No cierra la clase**: quedan nombrados en ADR-0029 los gates de frontera, wrapper, pineo de acciones y aislamiento con el mismo defecto. ADR-0029, bitácora 0027. |
| **El logout que no borraba la clave** | ✅ 2026-09-21 | **Punto 2 de la auditoría: ADR-0014 punto 3 nunca estuvo implementado.** El logout llamaba a `tokenStore.clear()` → `File.delete()` de **un** archivo; el alias `lume_session_tier1` seguía vivo en AndroidKeyStore, y `wipe()` —lo único que borra namespace y clave— tenía **un solo llamador en producción**: el sentinel de instalación. **Dos tests verdes lo cubrían**: el que assertea el alias llamaba a `wipe()` (camino que producción no tomaba), y el que sí tomaba el camino real **no asserteaba ni los archivos ni el alias pese a prometer ambos en su nombre**. Octavo verde-por-razón-equivocada; primero con la afirmación falsa en **tres ADRs** a la vez (0014, 0009, 0022 — las tres corregidas). La pista estaba en la última línea de la propia ADR-0014 y nadie comparó. Construido `core/session/LogoutContract.kt` (misma forma que `enforceInstallBoundary`, porque la secuencia estaba **inline en la composable** donde ningún test llegaba): borra el **namespace y la clave**, **cada paso se intenta aunque el anterior lance** (antes el primer throw dejaba vivos la clave tier-2 y la ventana de bloqueo con la UI ya en Login), y corre bajo **`NonCancellable`** (antes el scope del shell moría con la composición y truncaba el borrado). **Medido con control en vivo**: 4 tests nuevos con 3 cebos rojos cada uno por el test correcto, y **4/4 en emulador real** — restaurado el defecto, el test de device falla con «UNLOCK_CHALLENGE survived the production logout». Gate `check-logout-contract.sh` dirigido por el enum + **4 cebos** (el ensayo pasa de 15 a 19). **Fuera de alcance, declarado**: el rechazo de refresh sigue borrando sólo la entrada de tokens — es política del autor, no implementación. ADR-0014 enmendada, bitácora 0028. |
| S0.3 (design system) | ⏸️ | **No se toca**: espera el veredicto del autor sobre LumeUIComposer (WORKPLAN). |
| S1.1 (shell + auth) | 🟡 2026-08-21 | **Mitad sin UI construida.** Pedido de contrato escrito (`backend-requests/0001`) — **RESPONDIDO el 2026-08-21: el backend aceptó `ADR-0036 del backend` (audiencia separada por app en Identity Platform).** El alcance se aplica en su `AuthGuard` (no en su guard de roles, que no corre para `/v1/me`) y también en su handshake WS, deny-by-default con registro exhaustivo, y fuera de alcance responde **404** sin anunciar nada. Dos cosas que esta app debe saber: el `≤15 min` de vida del token sigue siendo **doctrina y no configuración** allá (no hay proyecto GCP), y falta **verificación externa** de que una segunda audiencia emita el mismo claim `sign_in_second_factor: totp` que su verificador exige en cada request. Falta construirlo, no decidirlo. **`core/session` hecho**: `KeychainSecureStore` (accesibilidad por target compilado) + `KeystoreSecureStore` (ADR-0009: Keystore AES-GCM sobre archivos, cero deps) + `SessionManager` single-flight con logout-wipe pinneado + `InactivityLock` fail-closed — **21 tests × 2 targets verdes**; los roundtrips de los stores de hardware se verifican en device (asimetrías declaradas, bitácora 0007). Quedan: `RefreshClient` HTTP (contrato), tier 2 biométrico, sentinel iOS, UI login. **Shell mínimo construido (2026-08-21)**: máquina de navegación `resolveDestination` (Login/Locked/Home, 3 tests × 2 targets), screens placeholder sin estilo (S0.3 diferido), store real cableado por `expect/actual @Composable rememberSecureStore` en `core/`, `SessionManager` con `UnwiredRefreshClient` (fail-closed hasta el cliente HTTP). Corre en emulador Android (APK instalado, MainActivity al frente). Bitácora 0009. |
| Resto de Fase 1 (S1.2–S1.5) | ⬜ | Sin empezar. Cada slice de lectura (agenda S1.3, contactos S1.4) trae su propio pedido de contrato con sus preguntas T11/T16. **Dato que S1.3 necesita antes de diseñar (registrado por el backend el 2026-08-21, con autorización del autor): la agenda del día NO EXISTE como ruta.** El contrato sólo tiene citas **por paciente** (`GET /v1/orgs/{tenantId}/patients/{patientId}/appointments`); no hay ninguna ruta de citas a nivel de tenant ni de disponibilidad/slots. `GET /v1/orgs/{tenantId}/appointments?professionalId&from&to` hay que **construirla**, y su deuda está anotada en el tablero (§3.1, «Deuda que este repo le debe»). Y cuando se construya es exactamente el escape de T11: es una ruta tenant-scoped sin `{patientId}`, de las que el interceptor de restricción de tratamiento no evalúa. |
| **Programa de fortificación** | 🟡 2026-08-21 | Plan aprobado: `docs/security/fortification-plan.md` (23 slices, 8 fases, mapeados al threat model). **F1 ✅ núcleo** (captura de pantalla/multitarea: FLAG_SECURE app-wide + cover iOS Compose + tapjacking, gate `check-screen-security.sh`, ADR-0010, bitácora 0008; cover iOS de host diferido al host Xcode). **F1 VERIFICADO EN DEVICE (Android, 2026-08-21)**: con la app al frente en el emulador, `screencap` privilegiado devuelve negro puro (0 píxeles con contenido) mientras el launcher captura normal (luma 82.5) — FLAG_SECURE bloqueando de verdad, no solo declarado (bitácora 0009). **F4 🟡 2026-08-21**: bloqueo por inactividad + desbloqueo biométrico **anclado a material de clave, jamás un booleano** (ADR-0011) — EC en Keystore firmando un reto (Android) / Keychain `.biometryCurrentSet` (iOS), clave destruida al cambiar el enrolamiento; `SessionLock` con 13 tests × 2 targets; gate `check-biometric-contract.sh` **ensayado con 8 cebos**; y **4 tests instrumentados verdes en Android real** (`UnlockKeyContractTest`) confirmando por `KeyInfo` que las 4 propiedades quedaron aplicadas de verdad. **Y la propiedad del tier PROBADA en device con control** (`Scripts/verify-tier2-invalidation.sh`: crear clave → control que DEBE fallar → enrolar huella nueva → la misma clave destruida). Falta sólo que la app *navegue* hasta `Locked` (necesita login) y el lado iOS (sin host). Bitácoras 0010 y 0014. **F2 ✅** (ninguna superficie pre-auth existe + `shared/PushSignal` **sin campo de texto**, gate con 8 cebos — ADR-0012, bitácora 0011). **F3 ✅** (la app no ofrece copiar + una sola pieza de entrada `core/input/SensitiveTextField` con endurecimiento por propósito, gate con 7 cebos — ADR-0013, bitácora 0013). **FASE A CERRADA**. **F3 reabierto y re-cerrado el 2026-08-25** (ADR-0024, bitácora 0022): los tres canales de exportación de **estructura** del SO, que no son la captura de pantalla y que FLAG_SECURE no gobierna. **Autofill estaba abierto** — todo campo de texto de Compose publica `ContentDataType.Text` sin condición ni opt-out, así que «campos donde esta app recibe datos personales» y «nodos entregados al servicio de autofill del usuario» eran el mismo conjunto. Se cierra desde el **ancestro**: `AndroidComposeView` sobreescribe `getImportantForAutofill()` a YES sin campo de respaldo, y el arreglo obvio **se descarta en silencio** (séptimo caso de algo que se ve aplicado sin estarlo, primero cazado antes de embarcar). **Content capture ya estaba cerrado por FLAG_SECURE** y se le agrega la bandera propia e independiente — declarada como tal, no como el arreglo. **Assist está vacío por construcción**. El gate se amplió a **allowlist** después de que su propio cebo lo cruzara con un calificador largo (mismo error de clase que F20) y se re-cebó en 7 grafías. **Probado en device con cebo**: sin el arreglo, 2 tests rojos. Residual declarado: petición manual, modo de compatibilidad y PCC re-admiten la vista. **F13 ✅** (higiene de URLs; pinning re-evaluado y **dejado como decisión tuya**) y **F20 ✅** (cadena de suministro: siete huecos medidos, incl. que el allowlist admitía una API de datos clínicos, que el classpath de plugins no tenía ningún control, y que el `gradle-wrapper.jar` —la raíz de confianza— era de otra versión). **F12 ✅ 2026-08-21** (transporte): cerró **cinco defectos vivos en código propio** — la caché de NSURLSession escribía **el bearer token** a disco en claro; el log «redactado» conservaba el path donde van los ids de paciente; el guard verificaba el protocolo y **no el origen** (una ruta que parece relativa, `//evil.test/steal`, se llevaba el token a otro host **sin aparecer en el log**); las excepciones de transporte llevaban la URL con su query; y la exclusión `**/core/**` de detekt desactivaba los gates en cualquier carpeta llamada `core`. Android ya declara cleartext negado **y anclas de confianza sólo del sistema**. Gate sobre el manifiesto **FUSIONADO** (el único que ve lo que inyectan las dependencias: `INTERNET` lo trae okhttp, `USE_FINGERPRINT` biometric). **Cuatro afirmaciones falsas de la doc corregidas** y cuarta asimetría de plataforma añadida. ADR-0016, bitácora 0017. **Fase B: F5 ✅** (logout verificado contra el Keystore real) **y F6 ✅** — F6 **encontró un agujero vivo**: `allowBackup=false` **no** cubre la migración entre teléfonos a targetSdk≥31, y nuestro paquete filtraba datos por ahí (medido, con control). Cerrado con `dataExtractionRules`, gate y verificación en device con control en vivo (ADR-0015, bitácora 0016). De paso se corrigieron dos afirmaciones falsas de la doc sobre iOS y un defecto real en `KeystoreSecureStore.get()` que habría crasheado al arrancar tras un reseteo de credenciales; colas: cover iOS de host y veto de teclados iOS (esperan el host Xcode), end-to-end de F4 (espera el login). **14 gates en CI, 2 verificadores en device con control, 99 tests en `commonTest`/`iosTest` (corren en Android host y en iOS) + 14 instrumentados en device.** |
| Fase 2 (lado paciente) | 🔓/⬜ 2026-08-21 | **El gate de ADR-0006 se abrió: el backend ACEPTÓ `ADR-0035` del backend** (tier de identidad de paciente). Distinción que importa: **la decisión existe, la superficie no** — el backend declara «falta construirlas, no decidirlas», así que no hay contra qué construir todavía (y ADR-0006 punto 1 prohíbe cablear incluso contra mocks hasta que exista). Lo decidido: el paciente es **Viewer** — edita foto, correo, teléfono y **dirección**, y agenda/cancela sus propias citas; tenant de IdP separado, enrolamiento mediado por la clínica, **TOTP obligatoria también para pacientes**. Cuando abra, la política de sesión del tier aterriza en un sucesor de ADR-0003 (ADR-0006 punto 3). **Actualización 2026-09-15:** la superficie también llegó — el backend publicó el encargo saliente `docs/backend-responses/0003-patient-tier-ready.md`, con una sección dirigida al agente de este repo. ADR-0006 quedó marcada como **superada**; lo único que sigue vigente de ella es su punto 3. Las cifras del contrato viven en el tablero del backend y no se copian acá. |

## Decisiones abiertas (autor)

- **¿Se estrecha el denylist de dependencias para permitir FCM?** *(nueva — 2026-08-21, hallazgo de
  F2, verificado con evidencia)*. El push de Android **sólo** existe vía
  `com.google.firebase:firebase-messaging`, y este repo lo rechaza por dos gates propios (denylist
  de Firebase y de Play Services, §8.1) más `ForbiddenImport` en detekt. El §8.1 escribió esa regla
  contra Analytics/Crashlytics; FCM es otro producto por la misma puerta. **Mientras no se decida,
  el push en Android es estructuralmente imposible.** El de iOS (APNs) no tiene colisión, así que un
  slice de push podría salir sólo en iOS. Ver ADR-0012 y bitácora 0011.
- **¿`specialtyCode` entra en la frontera?** *(nueva — 2026-09-07, leyendo `listMyAppointments` del contrato 0.32.0)*. La respuesta de citas del paciente trae `specialtyCode`. Nuestro §1.0 admite «existencia, fecha, hora, lugar/modalidad, con quién» y **la especialidad no está** — y la propia constitución dice, en su párrafo honesto, que **una cita con un especialista revela información de salud**. Es del titular sobre sí mismo, no una fuga a un tercero, pero significa que el dispositivo **guarda** ese dato. Tres salidas: (a) leerlo como parte de «con quién», (b) admitirlo explícitamente con su ADR como se hizo con la dirección, o (c) descartarlo en el mapper. **No se resuelve dentro de un slice de agenda.**
- **~~¿La dirección entra en la frontera de datos?~~ RESUELTA 2026-08-25 — ADR-0027.** Entra, **geolocalizada**, y el motor es **Google** por decisión del autor. La app **no habla con Google**: pregunta al backend, que guarda la llave y contrata la transferencia — que es literalmente lo que `ADR-0030 de LumeMed` llamó «el camino correcto» y difirió por no existir el endpoint. Falla a *tecleado*, nunca a *resuelto*: la capacidad es obligatoria, el resultado no (la lectura estricta se descartó a propósito — en zonas rurales sería una **exclusión**, no una validación). No deroga la denylist de `com.google.android.gms`: yendo por el backend no hay SDK de mapas en ningún binario, y hay **una sola implementación en `commonMain`** en vez de dos adaptadores. `CLAUDE.md` §1.0 y §0 actualizados en el mismo cambio; pedido `backend-requests/0005`, que desbloquea a las dos apps.
- **¿Se reabre la decisión de pinning?** *(nueva — 2026-08-21, F13/ADR-0017)*. ADR-0004 lo difirió porque «ataca sólo T5, el nivel menos probable». **Esa premisa ya no calza**: F12 estableció que iOS confía en las CAs que instala el usuario, así que en iOS es un problema de T1/T2 — los niveles que esta app pone primero. Pero **pinning hoy es imposible**: no hay certificado de producción ni host iOS donde ponerlo. Hay una opción intermedia (iOS puede distinguir ancla de sistema de CA de usuario) que cerraría la asimetría sin costo de rotación, con evidencia **de simulador y sin verificar**. Yo no lo decido: ver ADR-0017 Parte 2 con las cuatro recomendaciones.
- Nombre público de las tiendas (junto con el de LumeMed).

Resueltas: la fila en `ECOSYSTEM-STATUS.md` — autorizada por el autor y agregada el 2026-08-20
(§3.1 del tablero, commit `f5fd53c` del backend). **Cuándo pedir el ADR del tier paciente** —
resuelta el 2026-08-21: el autor lo decidió y el backend aceptó `ADR-0035`; ver la fila de Fase 2.
